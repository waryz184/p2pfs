# Binaire compilé (anchoré à la racine : ne doit PAS matcher cmd/p2pfs/)
/p2pfs
/vaultd

# Données runtime (coffres chiffrés locaux — jamais dans le repo)
vaultdata/

# Artefacts de build
*.tmp
*.tmp.*

# macOS
.DS_Store

# Docker Compose (contient le domaine, pas un secret mais pas à committer)
.env
